apiVersion: v1
kind: Service
metadata:
  name: project-svc
spec:
  type: NodePort
  selector:
    app: project
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 3000
      nodePort: 30080
